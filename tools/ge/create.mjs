import { defineCommand } from "citty";
import pc from "picocolors";
import { common, emit, guarded, out, ui } from "./shared.mjs";
import { createFromLibrary } from "@ge/blueprint-library";
function renderReceipt(r){ const d=r.data; out(`${pc.green("✓")} Resolved blueprint ${d.blueprint}`); if(d.dryRun){ out(`${pc.yellow("▲")} Dry run — no files written`); } else { out(`${pc.green("✓")} Copied OKF contract to ${d.workspace}/okf/agent.okf.md`); out(`${pc.green("✓")} Validated contract`); out(`${pc.green("✓")} Generated ${d.workspace} workspace`); out(`${pc.green("✓")} Generated source-system twins`); out(`${pc.green("✓")} Generated evals`); if(d.smoke) out(`${pc.green("✓")} Ran smoke proof`); } out("Workspace:"); out(`  ${d.workspace}`); out("Inspect:"); for(const a of r.artifacts.slice(0,8)) out(`  ${a.path}`); out(ui.next(r.nextCommand)); }
export const create = defineCommand({ meta:{name:"create",description:"Create an agent workspace from the Agent Library, workflow, OpenAPI, or blank contract"}, args:{...common, "from-library":{type:"string",description:"Library blueprint slug"}, out:{type:"string"}, overlay:{type:"string"}, target:{type:"string",default:"adk"}, "dry-run":{type:"boolean"}, "no-smoke":{type:"boolean"}, force:{type:"boolean"}}, run: guarded(async({args})=>{ if(!args["from-library"]) throw new Error("usage: ge create --from-library <slug> --out <dir>"); const result=await createFromLibrary({ slug:args["from-library"], outDir:args.out, overlay:args.overlay, target:args.target, dryRun:args["dry-run"], noSmoke:args["no-smoke"], force:args.force }); emit(args,result,renderReceipt); }) });
