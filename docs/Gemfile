# GitHub Pages renders this site automatically — no local build is required.
# This Gemfile only exists so you can preview the site locally with the same
# toolchain GitHub Pages uses:  cd docs && bundle install && bundle exec jekyll serve
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
