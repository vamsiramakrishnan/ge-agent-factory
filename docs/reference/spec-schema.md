---
title: Contract schema
parent: Reference
nav_order: 3
layout: default
description: The Enterprise Agent Contract's schema — generationSpec, behaviorContract, and architecture.pipeline — with field tables generated from the zod source of truth.
---

# Contract schema (use-case spec)

The use-case spec is the [Enterprise Agent Contract](../concepts/enterprise-agent-contract.html)
in its canonical JSON form — the contract the whole factory reads from. A built sample
catalog lives at
[`apps/factory/generated/use-cases.generated.json`](https://github.com/vamsiramakrishnan/ge-agent-factory)
(git-ignored, regenerated by `mise run catalog`); baseline specs are emitted by
[`apps/factory/scripts/emit-baseline-specs.mjs`](https://github.com/vamsiramakrishnan/ge-agent-factory).
The shape authority is `packages/agent-spec/src/schema.ts` (`@ge/agent-spec`) —
the field tables on this page are generated from those zod schemas by
`packages/agent-spec/scripts/gen-spec-reference.mjs` (`bun run docs:spec-ref`).

A spec entry has three top-level parts:

```jsonc
{
  "generationSpec":   { /* data shape + validation */ },
  "behaviorContract": { /* agent constraints, tools, workflow, evals */ },
  "architecture":     { /* system connections + pipeline stages */ }
}
```

Examples below are drawn from the `account-reconciliation-agent` entry.

---

## `generationSpec`

Describes the data the agent works over and how it is packaged.

<!-- BEGIN GENERATED: spec-schema-generation-spec — do not edit; run `bun run docs:spec-ref` -->
All object shapes below are loose: unknown extra keys are allowed, not rejected.

| Field | Type | Required | Constraints |
|---|---|---|---|
| `version` | number | yes | literal `1` |
| `rowPolicy` | object | yes |  |
| `sourceSystems[]` | object[] | yes | min 1 item |
| `entities[]` | object[] | yes | min 1 item |
| `documents[]` | object[] | yes | min 1 item |
| `anomalies[]` | object[] | yes | min 1 item |
| `datastorePackaging` | object | yes |  |
| `validation` | object | yes |  |
| `relationships[]` | object[] | no |  |
| `apis[]` | object[] | no |  |
| `behaviorContract` | object | yes | see [`behaviorContract`](#behaviorcontract) |
| `variantOf` | object | no |  |
| `bindings` | object | no |  |
| `provenance` | object | no |  |

**`generationSpec.rowPolicy`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `defaultRowsPerEntity` | number | yes |  |
| `minimumRowsPerEntity` | number | yes |  |
| `rationale` | string | yes | min length 1 |
| `seed` | number | no |  |

**`generationSpec.sourceSystems[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | yes |  |
| `name` | string | yes |  |
| `owns[]` | string[] | yes | min 1 item |
| `protocol` | string | yes | min length 1 |
| `localBacking[]` | string[] | yes | min 1 item |
| `toolNames[]` | string[] | yes | min 1 item |
| `mcpToolNames[]` | string[] | no |  |
| `evidence[]` | enum[] | yes | min 1 item; each one of `sql_result`, `source_system_record`, `document_reference`, `generated_audit_trail`, `api_response` |

**`generationSpec.entities[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `name` | string | yes |  |
| `sourceSystemId` | string | no |  |
| `datastore` | enum | yes | one of `alloydb`, `firestore`, `bigtable`, `bigquery`, `cloud-storage`, `json-api`, `mcp` |
| `rowCount` | number | yes |  |
| `primaryKey` | string | yes |  |
| `columns[]` | object[] | yes | min 3 items |

**`generationSpec.entities[].columns[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `name` | string | yes |  |
| `type` | string | yes |  |
| `required` | boolean | no |  |
| `values[]` | string[] | no |  |
| `weights[]` | number[] | no |  |
| `ref` | string | no |  |
| `min` | string or number | no |  |
| `max` | string or number | no |  |
| `decimals` | number | no |  |
| `trueRate` | number | no |  |
| `unit` | string | no |  |
| `range` | object | no |  |
| `piiClass` | enum | no | one of `none`, `personal`, `sensitive`, `regulated` |
| `maskingPolicy` | string | no |  |

**`generationSpec.entities[].columns[].range`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `min` | number | yes |  |
| `max` | number | yes |  |

**`generationSpec.documents[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | yes |  |
| `title` | string | yes |  |
| `requiredSections[]` | string[] | yes | min 1 item |
| `citationAnchors[]` | string[] | yes | min 1 item |
| `minimumWordCount` | number | yes |  |
| `sourceSystemId` | string | no |  |
| `type` | enum | no | one of `policy`, `report`, `sop`, `contract`, `knowledge_base`, `audit`, `memo`, `guide`, `runbook`, `playbook` |
| `linkedEntities[]` | string[] | no |  |

**`generationSpec.anomalies[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `description` | string | yes |  |
| `affectedEntities[]` | string[] | yes | min 1 item |
| `expectedEvidence[]` | string[] | yes | min 1 item |
| `id` | string | no |  |
| `discoveryPath[]` | string[] | no |  |
| `expectedRecommendation` | string | no |  |

**`generationSpec.datastorePackaging`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `alloydb` | object | no |  |
| `firestore` | object | no |  |
| `bigtable` | object | no |  |
| `bigquery` | object | no |  |
| `cloudStorage` | object | no |  |
| `apis` | object | no |  |

**`generationSpec.datastorePackaging.alloydb`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `database` | string | yes |  |
| `schemas[]` | string[] | yes |  |

**`generationSpec.datastorePackaging.firestore`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `database` | string | yes |  |
| `collections[]` | string[] | yes |  |

**`generationSpec.datastorePackaging.bigtable`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `instance` | string | yes |  |
| `tables[]` | string[] | yes |  |

**`generationSpec.datastorePackaging.bigquery`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `dataset` | string | yes |  |
| `tables[]` | string[] | yes |  |

**`generationSpec.datastorePackaging.cloudStorage`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `bucketSuffix` | string | yes |  |
| `prefixes[]` | string[] | yes |  |

**`generationSpec.datastorePackaging.apis`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `serviceName` | string | yes |  |
| `deploymentTarget` | enum | yes | one of `cloud_run`, `local` |

**`generationSpec.validation`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `smokePrompt` | string | yes | min length 1 |
| `assertions[]` | string[] | yes | min 2 items |
| `expectedAnswer[]` | string[] | no |  |

**`generationSpec.relationships[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `from` | string | yes |  |
| `to` | string | yes |  |
| `cardinality` | enum | yes | one of `one-to-one`, `one-to-many`, `many-to-one`, `many-to-many` |
| `orphanPolicy` | enum | yes | one of `none`, `seeded-anomaly`, `allowed` |

**`generationSpec.apis[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `method` | enum | yes | one of `GET`, `POST`, `PUT`, `PATCH`, `DELETE` |
| `path` | string | yes |  |
| `requestSchema` | record<string, string> | yes |  |
| `responseSchema` | record<string, string> | yes |  |
| `systemId` | string | no |  |
| `sourceSystemId` | string | no |  |
| `operation` | string | no |  |
| `id` | string | no |  |
| `description` | string | no |  |
| `fixture` | string | no |  |
| `mcpToolName` | string | no |  |
| `idempotencyKey` | string | no |  |

**`generationSpec.variantOf`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `baseId` | string | yes |  |
| `kind` | enum | yes | one of `vertical`, `source-swap`, `custom` |

**`generationSpec.bindings`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `systems` | record<string, string> | no |  |
| `terminology` | record<string, string> | no |  |
| `policyOverlays[]` | object[] | no |  |
| `workflowOverrides` | object | no |  |

**`generationSpec.bindings.policyOverlays[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `kind` | enum | yes | one of `refusal`, `escalation` |
| `rule` | string or object | yes |  |

**`generationSpec.bindings.workflowOverrides`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `mode` | enum | no | one of `sequential`, `parallel` |
| `steps[]` | object[] | no |  |

**`generationSpec.bindings.workflowOverrides.steps[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | yes |  |
| `label` | string | no |  |
| `description` | string | no |  |
| `tools[]` | string[] | no |  |
| `remove` | boolean | no |  |

**`generationSpec.provenance`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `origin` | enum | yes | one of `interview`, `deck`, `migration`, `variant`, `manual` |
| `sourceRef` | string | no |  |
| `version` | string or number | no |  |
| `owner` | string | no |  |
| `status` | enum | no | one of `draft`, `registered`, `promoted`, `retired` |
| `createdAt` | string | no |  |
| `lineage[]` | string[] | no |  |
<!-- END GENERATED: spec-schema-generation-spec -->

**`sourceSystems[]`** example:

```json
{
  "id": "sap_s_4hana_fi",
  "name": "SAP S/4HANA FI",
  "owns": ["gl_entries", "subledger_balances", "open_items"],
  "protocol": "RFC/BAPI",
  "localBacking": ["alloydb"],
  "toolNames": ["query_sap_s_4hana_fi_gl_entries"],
  "evidence": ["source_system_record", "generated_audit_trail"]
}
```

**`entities[].columns[]`** — typed columns, e.g. `{"name":"amount","type":"float","min":-50000,"max":50000,"decimals":2,"required":true}`. Column types include `seq`, `date`, `enum` (with `values`), `float`/`number`, `boolean`, `ref:TABLE.COL`, and faker types.

---

## `behaviorContract`

The behavior contract is what turns a generic data agent into a task-specific one.
It is **required at build time** for the factory to generate a real agent, and
`toolIntents[]` must include at least one `query` and one `evidence_lookup`
intent (a zod refinement in the schema, not visible in the tables below).

<!-- BEGIN GENERATED: spec-schema-behavior-contract — do not edit; run `bun run docs:spec-ref` -->
All object shapes below are loose: unknown extra keys are allowed, not rejected.

| Field | Type | Required | Constraints |
|---|---|---|---|
| `role` | string | yes |  |
| `primaryObjective` | string | yes | min length 60 |
| `inScope[]` | string[] | yes | min 2 items |
| `outOfScope[]` | string[] | yes | min 2 items |
| `toolIntents[]` | object[] | yes | min 3 items |
| `evidenceRequirements[]` | object[] | yes | min 1 item |
| `escalationRules[]` | object[] | yes | min 2 items |
| `refusalRules[]` | string[] | yes | min 2 items |
| `goldenEvals[]` | object[] | yes | min 1 item |
| `workflow` | object | no |  |
| `answerableQueries[]` | object[] | no |  |
| `groundingContracts[]` | object[] | no |  |
| `toolContracts[]` | object[] | no |  |
| `refusalPolicies[]` | object[] | no |  |
| `escalationPolicies[]` | object[] | no |  |
| `personas[]` | object[] | no |  |
| `errorPathBehavior[]` | object[] | no |  |
| `slos` | object | no |  |
| `capabilityDependencies[]` | object[] | no |  |

**`behaviorContract.toolIntents[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `name` | string | yes |  |
| `kind` | enum | yes | one of `query`, `action`, `evidence_lookup`, `notification`, `calculation` |
| `description` | string | yes |  |
| `requiredInputs[]` | string[] | yes | min 1 item |
| `produces[]` | string[] | yes | min 1 item |
| `evidenceEmitted[]` | enum[] | yes | min 1 item; each one of `sql_result`, `source_system_record`, `document_reference`, `generated_audit_trail`, `api_response` |
| `sourceSystemId` | string | no |  |

**`behaviorContract.evidenceRequirements[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `claim` | string | yes |  |
| `mustCite[]` | string[] | yes |  |
| `sourceSystemIds[]` | string[] | yes |  |

**`behaviorContract.escalationRules[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `trigger` | string | yes |  |
| `action` | enum | yes | one of `escalate_to_human`, `refuse`, `request_more_info`, `use_fallback_tool` |
| `rationale` | string | yes |  |
| `handoffTarget` | string | no |  |

**`behaviorContract.goldenEvals[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | yes |  |
| `prompt` | string | yes |  |
| `expectedToolCalls[]` | string[] | yes |  |
| `mustReferenceEntities[]` | string[] | no |  |
| `mustCiteDocuments[]` | string[] | no |  |
| `expectedActionOutcome` | string | no |  |
| `expectedBehaviors[]` | string[] | no |  |
| `forbiddenBehaviors[]` | string[] | no |  |
| `mechanisms[]` | string[] | no |  |

**`behaviorContract.workflow`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `mode` | enum | yes | one of `sequential`, `parallel` |
| `steps[]` | object[] | yes |  |

**`behaviorContract.workflow.steps[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | no |  |
| `label` | string | yes |  |
| `description` | string | no |  |
| `tools[]` | string[] | yes |  |

**`behaviorContract.answerableQueries[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | no |  |
| `request` | string | yes |  |
| `tools[]` | string[] | yes |  |
| `evidence[]` | string[] | no |  |
| `stage` | string | no |  |

**`behaviorContract.groundingContracts[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `claimType` | string | yes |  |
| `evidence[]` | object[] | yes | min 1 item |
| `citationRequired` | boolean | yes |  |

**`behaviorContract.groundingContracts[].evidence[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `system` | string | yes |  |
| `tool` | string | no |  |
| `entity` | string | no |  |
| `field` | string | no |  |

**`behaviorContract.toolContracts[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `tool` | string | yes |  |
| `preconditions[]` | string[] | yes |  |
| `postconditions[]` | string[] | yes |  |
| `idempotency` | enum | yes | one of `safe`, `idempotent`, `effectful` |
| `confirmationPolicy` | enum | yes | one of `never`, `destructive`, `always` |

**`behaviorContract.refusalPolicies[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | no |  |
| `trigger` | object | yes |  |
| `response` | string | yes |  |
| `rationale` | string | yes |  |

**`behaviorContract.refusalPolicies[].trigger`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `kind` | enum | yes | one of `entity_condition`, `request_category`, `authority_missing`, `data_sensitivity` |
| `entity` | string | no |  |
| `condition` | string | no |  |
| `category` | string | no |  |
| `authority` | string | no |  |
| `sensitivity` | enum | no | one of `none`, `personal`, `sensitive`, `regulated` |

**`behaviorContract.escalationPolicies[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | no |  |
| `trigger` | object | yes |  |
| `response` | string | yes |  |
| `rationale` | string | yes |  |
| `handoffTarget` | string | no |  |

**`behaviorContract.escalationPolicies[].trigger`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `kind` | enum | yes | one of `entity_condition`, `request_category`, `authority_missing`, `data_sensitivity` |
| `entity` | string | no |  |
| `condition` | string | no |  |
| `category` | string | no |  |
| `authority` | string | no |  |
| `sensitivity` | enum | no | one of `none`, `personal`, `sensitive`, `regulated` |

**`behaviorContract.personas[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `id` | string | yes |  |
| `role` | string | yes |  |
| `goals[]` | string[] | yes | min 1 item |
| `vocabulary` | string | no |  |
| `patience` | enum | no | one of `low`, `medium`, `high` |
| `adversarial` | boolean | no |  |
| `simulationInstruction` | string | no |  |

**`behaviorContract.errorPathBehavior[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `failureMode` | enum | yes | one of `rate_limited`, `timeout`, `conflict`, `validation_error`, `not_found`, `permission_denied`, `unavailable` |
| `behavior` | enum | yes | one of `retry`, `inform`, `escalate`, `degrade` |
| `maxRetries` | number | no |  |
| `fallback` | string | no |  |
| `toolOverrides` | record<string, object> | no |  |

**`behaviorContract.slos`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `taskSuccess` | object | no |  |
| `latency` | object | no |  |
| `containment` | object | no |  |

**`behaviorContract.slos.taskSuccess`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `definition` | string | yes |  |
| `target` | number | yes |  |

**`behaviorContract.slos.latency`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `p95TtftMs` | number | no |  |
| `p95FullMs` | number | no |  |

**`behaviorContract.slos.containment`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `target` | number | yes |  |

**`behaviorContract.capabilityDependencies[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `capability` | string | yes |  |
| `requires[]` | string[] | yes | min 1 item |
| `fallback` | enum | no | one of `degrade`, `refuse`, `escalate` |
| `fallbackDetail` | string | no |  |
<!-- END GENERATED: spec-schema-behavior-contract -->

**`toolIntents[]`** example:

```json
{
  "name": "action_sap_s_4hana_fi_close",
  "kind": "action",
  "sourceSystemId": "sap_s_4hana_fi",
  "description": "Execute the close step after evidence is gathered and gates pass.",
  "requiredInputs": ["target_id", "rationale"],
  "produces": ["action_id", "audit_record_id"],
  "evidenceEmitted": ["api_response", "generated_audit_trail"]
}
```

### `behaviorContract.workflow`

The multi-agent topology, derived from `architecture.pipeline` + `toolIntents` by
`buildWorkflowFromPipeline` in
[`apps/factory/scripts/factory/agent-workflow.mjs`](https://github.com/vamsiramakrishnan/ge-agent-factory).
It is **only emitted when the pipeline has ≥2 tool-bearing stages spanning ≥2
distinct tools** — otherwise the agent is single-agent and `workflow` is omitted.

```json
{
  "mode": "sequential",
  "steps": [
    {
      "id": "balance__document__pull",
      "label": "Balance & Document Pull",
      "description": "Pull GL balances and sub-ledger details; match against BlackLine templates.",
      "tools": ["query_sap_s_4hana_fi_gl_entries", "query_blackline_reconciliations"]
    }
  ]
}
```

- **`mode`**: parallel only when `architecture.parallel === true` or every
  stage is `parallel: true`; otherwise sequential.
- **`steps[]`**: the factory maps `sequential` → ADK `SequentialAgent`,
  `parallel` → `ParallelAgent`, with one sub-agent per step scoped to that
  step's `tools`.

### `goldenEvals[]`

```json
{
  "id": "account-reconciliation-agent-end-to-end",
  "prompt": "Run the workflow for the current period; cite evidence and surface escalations.",
  "expectedToolCalls": ["query_sap_s_4hana_fi_gl_entries", "action_sap_s_4hana_fi_close"],
  "mustReferenceEntities": ["gl_entries", "reconciliations"],
  "mustCiteDocuments": ["account-reconciliation-agent-controls-playbook"],
  "expectedActionOutcome": "Close executed against SAP S/4HANA FI with an audit-trail entry.",
  "forbiddenBehaviors": ["do not invent KPI numbers", "do not skip evidence_lookup"]
}
```

### `answerableQueries` and eval `mechanisms`

These are the two **capability concepts OKF (Open Knowledge Format) cares
about**. They may be authored
explicitly on the contract; if absent, they are **derived** by
[`apps/factory/scripts/lib/okf-capabilities.mjs`](https://github.com/vamsiramakrishnan/ge-agent-factory)
so older specs gain queries/tests without re-authoring:

- **`answerableQueries[]`** — `{ id, request, tools[], evidence[], stage? }`: the
  requests an agent can serve, each with the tools that serve it and the workflow
  stage it runs in. Derivation: explicit `answerableQueries` → else one per
  workflow **step** → else one per `inScope` item served by the read-tool surface
  (`deriveAnswerableQueries`).
- **eval `mechanisms`** — per golden eval, the tool names that **must** fire.
  Derivation: explicit `eval.mechanisms` → else the eval's `expectedToolCalls`
  (`evalMechanisms` / `deriveTestMechanisms`). Each derived test record carries
  `{ id, prompt, mechanisms[], rubric?, mustReferenceEntities, mustCiteDocuments, validates? }`,
  where `validates` links the test back to the Query Capability it exercises.

---

## `architecture`

<!-- BEGIN GENERATED: spec-schema-architecture — do not edit; run `bun run docs:spec-ref` -->
All object shapes below are loose: unknown extra keys are allowed, not rejected.

| Field | Type | Required | Constraints |
|---|---|---|---|
| `connections[]` | object[] | yes |  |
| `pipeline[]` | object[] | yes |  |

**`architecture.connections[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `system` | string | yes |  |
| `description` | string | yes |  |
| `direction` | enum | no | one of `read`, `write`, `bidirectional` |
| `protocol` | string | no |  |
| `dataExamples[]` | string[] | no |  |
| `category` | enum | no | one of `erp`, `analytics`, `ai`, `clm`, `market-data`, `collaboration` |

**`architecture.pipeline[]`**

| Field | Type | Required | Constraints |
|---|---|---|---|
| `label` | string | yes |  |
| `description` | string | no |  |
| `systems[]` | string[] | no |  |
| `layer` | enum | no | one of `integration`, `ml`, `llm` |
| `dataIn` | string | no |  |
| `dataOut` | string | no |  |
| `parallel` | boolean | no |  |
<!-- END GENERATED: spec-schema-architecture -->

`pipeline[]` is the source for `behaviorContract.workflow`, consumed at authoring time: `buildWorkflowFromPipeline` matches
each stage's label/description against the tool intents to produce the multi-agent
`workflow`.

> The `account-reconciliation-agent` sample lives in
> `apps/factory/generated/use-cases.generated.json` (under the app dir),
> not at the repo root.
{: .note }

---

## Adding a new spec field — the consumer checklist

The spec shape has a single schema module of record —
`packages/agent-spec/src/schema.ts` (`@ge/agent-spec`) — but the shipped
consumers are still **duck-typed readers**: each reads the fields it knows
about and silently ignores the rest. A new field added to the schema but not
threaded through the right files doesn't error — it quietly fails to reach
the OKF bundle, the generated workspace, or the round-trip back to a spec.
This checklist is the verified consumer set (as of 2026-07). Work through it
top to bottom; skip a row only when you can say why it doesn't apply.

### Where a spec is authored and assembled

| # | File | What it does with the spec | Touch it when… |
|---|---|---|---|
| 0 | `packages/agent-spec/src/schema.ts` (`@ge/agent-spec`) | The schema of record: zod schemas + inferred types for the whole spec shape (`GenerationSpecSchema`, `BehaviorContractSchema`, `AgentSpecEntrySchema`, …); the shipped quality validators live beside it in `packages/agent-spec/src/validate.mjs` | Always — add the field to the right schema first |
| 1 | `apps/presentation/src/types/architecture.ts` | Re-export shim over `@ge/agent-spec` for the spec-shape types (`UseCaseGenerationSpec`, `AgentBehaviorContract`, `UseCaseEntitySpec`); still home to presentation-only authoring interfaces | Only for presentation-side conveniences — spec-shape fields belong in row 0's schema |
| 2 | `apps/factory/scripts/sync-use-cases-from-slides.mjs` | Evaluates each slide's explicit `generationSpec` const into the build catalog (`mise run catalog` → `apps/factory/generated/use-cases.generated.json`), and **synthesizes** a fallback spec (`synthesizeGenerationSpec`) for slides without one | Explicit specs pass a new field through untouched; the *synthesized* fallback only has fields this file builds — add it there if fallback specs need it |
| 3 | `apps/factory/scripts/emit-baseline-specs.mjs` + `apps/factory/scripts/factory/baseline/{spec,behavior-contract}.mjs` | Programmatically emits baseline `generationSpec`/`behaviorContract` blocks into slide TSX | Baseline-emitted specs should carry the field |
| 4 | `apps/factory/src/agent-spec-registry.js` (`writeInterviewSpecEntry`) + `packages/std/src/spec-gaps.mjs` | Registers interview-authored specs under `apps/factory/catalog/interview-specs` and gates buildability via named gap codes (`missing_source_systems`, …) — the validators that emit them live in `packages/agent-spec/src/validate.mjs` (schema authority: `packages/agent-spec/src/schema.ts`), re-exported by the registry | The field is *required* for a buildable spec — add a gap code in `validate.mjs` (its byte-exact output is pinned by `packages/agent-spec/tests/parity.test.mjs`) and a message in `spec-gaps.mjs` |
| 5 | `apps/console/src/components/interview/artifacts/specArtifact.ts` (+ `SpecCanvas.tsx`) and `apps/console/src/server/interview-docs.mjs` | The console's interview-side spec types/labels, and the `/api/interviews/:id/okf` bundle built from the interview's `agent-spec.json` | The field is authored or displayed in the interview flow |

### Where a spec is consumed at build time

| # | File | What it does with the spec | Touch it when… |
|---|---|---|---|
| 6 | `apps/factory/scripts/factory/use-case/schema-from-generation-spec.mjs` | Turns explicit `generationSpec.entities`/`sourceSystems` into the generator's table plan — note it copies column attributes through an **explicit allowlist** (`name, type, values, weights, ref, min, max`) | Any new entity/column attribute — it is dropped here otherwise |
| 7 | `apps/factory/scripts/factory/use-case/schema-derivation.mjs` | Legacy heuristic derivation for use cases *without* an explicit `generationSpec` | The field should also exist on heuristically-derived specs |
| 8 | `apps/factory/scripts/factory/agent-workflow.mjs` (`buildWorkflowFromPipeline`) | Derives `behaviorContract.workflow` from `architecture.pipeline` + `toolIntents` | The field affects workflow topology |
| 9 | `apps/factory/scripts/lib/okf-capabilities.mjs` | Derives `answerableQueries` / test `mechanisms` from the contract when not explicit | The field feeds the capability spine (queries/tests) |
| 10 | `tools/lib/spec-review.mjs` and `apps/factory/scripts/audit-usecase-specs.mjs` | `ge`/console spec-review summary (`/api/ge/specs/review`) and the spec-maturity audit | The field should be surfaced or audited |

### The round-trip-critical pair

| # | File | Role |
|---|---|---|
| 11 | `apps/factory/scripts/spec-to-okf.mjs` (`buildBundle`) | **Renders** known spec fields into OKF concept markdown. A field it doesn't render never reaches the bundle |
| 12 | `apps/factory/scripts/okf-to-spec.mjs` (`okfToSpec`) | **Parses** the markdown back into a partial spec. A field must have a matching render ↔ parse pair (frontmatter key, `**Label:**` line, or section) or it is lost on the way back |
| 13 | `apps/factory/scripts/factory/agents/okf-artifacts.mjs` (`writeOkfArtifacts`) | Rebuilds an `okfSpec` from the workspace manifest with an **explicit field list** (`id, title, subtitle, persona, department, kpis, architecture` + `generationSpec.{behaviorContract, sourceSystems, entities, documents}`) before calling `buildBundle` — a new top-level field is dropped from the deployed agent's `app/knowledge/` bundle unless added here |

A field is **round-trip-critical** when it must survive spec → OKF → spec
(anything a re-imported BRD — Business Requirements Document — or the
interview OKF export depends on):
`behaviorContract.{role, primaryObjective, inScope, outOfScope, refusalRules,
toolIntents, workflow, answerableQueries, goldenEvals}` and
`generationSpec.{sourceSystems, entities, documents}` are the set today. If
your field belongs in that set, rows 11–12 are mandatory, and prefer carrying
it as **frontmatter** (like `source_id`) over prose — frontmatter round-trips
losslessly; prose parsing is best-effort.

> There is a known **open round-trip bug** in `spec-to-okf.mjs`: workflow step
> ids with double underscores come back single-underscored (see the
> `round-trip recovers tool names…` and `capability spine: …` entries and
> their `notes` in `tools/known-test-failures.json`). Those two failures are
> pre-existing and allowed by `bun run test:gated`; a *new* round-trip
> failure for your field is yours — don't add it to the known-failures list.
{: .warning }

### Verify

```bash
# unit + round-trip tests — extend these with your field
bun test apps/factory/scripts/spec-to-okf.test.mjs

# byte-exact golden for buildBundle (see AGENTS.md's golden-test policy):
# a diff here means your change altered emitted bytes — only regenerate
# (GE_UPDATE_GOLDEN=1 bun test apps/factory/tests/spec-to-okf-golden.test.js)
# after confirming the new output is intentional; also add your field to the
# representative SPEC in that test so the golden covers it
bun test apps/factory/tests/spec-to-okf-golden.test.js

# rebuild the catalog and convert a real spec end-to-end
mise run catalog
node apps/factory/scripts/spec-to-okf.mjs --id account-reconciliation-agent
node apps/factory/scripts/okf-to-spec.mjs --bundle apps/factory/artifacts/okf/account-reconciliation-agent/

# full gate — expect no NEWLY failing tests
bun run test:gated
```

Finally, document the field: the tables above on this page, and the
spec → OKF mapping table in the [Spec ⇄ OKF cookbook](../cookbooks/spec-to-okf.html)
if it maps to a concept.
