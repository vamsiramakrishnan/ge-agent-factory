// Moved to @ge/ui so the deck (apps/presentation) can share it. Re-exported here so the
// console's existing "../components/Lifecycle" importers are unchanged.
export { Lifecycle, type LifecycleProps } from "@ge/ui";
