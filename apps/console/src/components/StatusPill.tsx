// Moved to @ge/ui (shared across console + deck). Re-exported here so the console's
// existing "../components/StatusPill" importers (StatusPill, statusStyle, statusLabel) are unchanged.
export { StatusPill, statusStyle, statusLabel, type StatusValue } from "@ge/ui";
