{
  "name": "@ge/factory-install",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Plan-only install contract for GE factory Terraform, builds, and runtime environment wiring.",
  "exports": {
    ".": "./src/index.mjs",
    "./contract": "./install-contract.v1.json"
  },
  "bin": {
    "ge-factory-install": "./src/cli.mjs"
  },
  "scripts": {
    "check": "node --check src/index.mjs && node --check src/cli.mjs && node --check tests/install-contract.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "contract": "node src/cli.mjs contract"
  }
}
